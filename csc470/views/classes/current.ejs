<!DOCTYPE html>
<html lang="en">
<head> <% include ../partials/navbar %> </head>
<body>
	<div class="main"><div class="col-md-3"></div>
<div class="container-fluid col-md-6" >
<div class="">
<h2>Classes Left to Take</h2>
<table class="table table-striped table-condensed table-hover">
	<thead>
		<th>Class ID</th>
		<th>Class Name</th>
		<th>Status</th>
	</thead>
	<% for (var i = 0; i < classes_needed.length; i++){ %> <tr>
		<% var course_json = JSON.stringify(classes_needed[i]) %>
		<td><%= classes_needed[i]._subject %> <%= classes_needed[i]._courseNumber %></td>
		<td><%= classes_needed[i]._name %></td>
		<td>
			<div class="dropdown">
				<button class="btn btn-secondary btn-sm dropdown-toggle" type=button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopu="true" aria-expanded="false">
					Still Needed
				</button>
				<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
					<h6 class="dropdown-header">Mark as...</h6>
					<li>
						<form method="post" action="/classes/completed" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Completed</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/take" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >In Progress</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/register" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Registered</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/waitlist" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Waitlisted</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/untake" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Still Needed</button>
						</form>
					</li>
				</div>
			</div>
		</td>
	</tr><% } %>

	<% for (var i=0; i < user.classesFinished.length; i++){ %><tr>
		<% var course_json = JSON.stringify(user.classesFinished[i]) %>
		<td><%= user.classesFinished[i]._subject %> <%= user.classesFinished[i]._courseNumber %></td>
		<td><%= user.classesFinished[i]._name %></td>
		<td>
			<div class="dropdown">
				<button class="btn btn-secondary btn-sm dropdown-toggle" type=button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopu="true" aria-expanded="false">
					Completed
				</button>
				<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
					<h6 class="dropdown-header">Mark as...</h6>
					<li>
						<form method="post" action="/classes/completed" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Completed</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/take" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >In Progress</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/register" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Registered</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/waitlist" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Waitlisted</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/untake" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Still Needed</button>
						</form>
					</li>
				</div>
			</div>
		</td>
	</tr><% } %>

	<% for (var i=0; i < user.classesInProgress.length; i++){ %><tr>
		<% var course_json = JSON.stringify(user.classesInProgress[i]) %>
		<td><%= user.classesInProgress[i]._subject %> <%= user.classesInProgress[i]._courseNumber %></td>
		<td><%= user.classesInProgress[i]._name %></td>
		<td>
			<div class="dropdown">
				<button class="btn btn-secondary btn-sm dropdown-toggle" type=button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopu="true" aria-expanded="false">
					In Progress
				</button>
				<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
					<h6 class="dropdown-header">Mark as...</h6>
					<li>
						<form method="post" action="/classes/completed" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Completed</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/take" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >In Progress</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/register" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Registered</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/waitlist" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Waitlisted</button>
						</form>
					</li>
					<li>
						<form method="post" action="/classes/untake" class="inline">
						<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Still Needed</button>
						</form>
					</li>
				</div>
			</div>
		</td>
	</tr><% } %>

</table>
</div>
</div>
</div>
</body>
</html>

