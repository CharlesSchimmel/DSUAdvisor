<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/navbar %>
</head>
<body class="main" style="margin-top: 10vh;">
<div>
	<div class="page-header text-center">
		<h1><span class="fa"></span>Current Class Statuses</h1>
	</div>

	<ul class="list-group">

	<% var classList = user.classesInProgress %> <% var course_json = JSON.stringify(classList[i]) %>
	<% for (var i=0; i<classList.length; i++) { %>
	<div class="list-group-item col-md-10 col-md-push-1 col-md-pull-1">
		<div class="row bg-primary">
			<div class="col-md-4">
				<h4><%= classList[i]._name %></h4>
			</div> <!--- .col ---!>
			<div class="col-md-3 pull-right">
				<h4 class="text-right"><%= classList[i]._subject %> <%=classList[i]._courseNumber %></h4>
			</div> <!--- .col ---!>
		</div> <!--- .row ---!>

		<% if (classList[i]._preRequisites.length > 0) { %>
			<div class="row">
				<p class="col-md-push-1 col-md-10 text-justify">
					Prerequisites:
					<%= classList[i]._preRequisites %>
				</p> <!--.col -->
			</div>
		<% } %>

		<div class="row">
			<p class="col-md-10 text-justify col-md-push-1">
				<%= classList[i]._description %>
			</p> <!--.col -->
		</div><!-- row --> 
		<div class="dropdown">
			<button class="btn btn-secondary btn-sm dropdown-toggle" type=button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopu="true" aria-expanded="false">
				In Progress
			</button>
			<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
				<h6 class="dropdown-header">Mark as...</h6>
				<li>
					<form method="post" action="/classes/completed" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Completed</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/take" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >In Progress</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/register" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Registered</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/waitlist" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Waitlisted</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/untake" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Still Needed</button>
					</form>
				</li>
			</div>
		</div>
		</div>
	</div> <!-- list-group-item col-md-10 col-md-push-1 col-md-pull-1 -->
	<% } %>

	<% var classList = user.classesSignedUpfor %> <% var course_json = JSON.stringify(classList[i]) %>
	<% for (var i=0; i<classList.length; i++) { %>
	<div class="list-group-item col-md-10 col-md-push-1 col-md-pull-1">
		<div class="row bg-info">
			<div class="col-md-4">
				<h4><%= classList[i]._name %></h4>
			</div> <!--- .col ---!>
			<div class="col-md-3 pull-right">
				<h4 class="text-right"><%= classList[i]._subject %> <%=classList[i]._courseNumber %></h4>
			</div> <!--- .col ---!>
		</div> <!--- .row ---!>

		<% if (classList[i]._preRequisites.length > 0) { %>
			<div class="row">
				<p class="col-md-push-1 col-md-10 text-justify">
					Prerequisites:
					<%= classList[i]._preRequisites %>
				</p> <!--.col -->
			</div>
		<% } %>

		<div class="row">
			<p class="col-md-10 text-justify col-md-push-1">
				<%= classList[i]._description %>
			</p> <!--.col -->
		</div><!-- row --> 
		<div class="dropdown">
			<button class="btn btn-secondary btn-sm dropdown-toggle" type=button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopu="true" aria-expanded="false">
				Registered
			</button>
			<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
				<h6 class="dropdown-header">Mark as...</h6>
				<li>
					<form method="post" action="/classes/completed" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Completed</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/take" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >In Progress</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/register" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Registered</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/waitlist" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Waitlisted</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/untake" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Still Needed</button>
					</form>
				</li>
			</div>
		</div>
	</div> <!-- list-group-item col-md-10 col-md-push-1 col-md-pull-1 -->
	<% } %>


	<% var classList = user.classesWaitlisted %> <% var course_json = JSON.stringify(classList[i]) %>
	<% for (var i=0; i<classList.length; i++) { %>
	<div class="list-group-item col-md-10 col-md-push-1 col-md-pull-1">
		<div class="row bg-warning">
			<div class="col-md-4">
				<h4><%= classList[i]._name %></h4>
			</div> <!--- .col ---!>
			<div class="col-md-3 pull-right">
				<h4 class="text-right"><%= classList[i]._subject %> <%=classList[i]._courseNumber %></h4>
			</div> <!--- .col ---!>
		</div> <!--- .row ---!>

		<% if (classList[i]._preRequisites.length > 0) { %>
			<div class="row">
				<p class="col-md-push-1 col-md-10 text-justify">
					Prerequisites:
					<%= classList[i]._preRequisites %>
				</p> <!--.col -->
			</div>
		<% } %>

		<div class="row">
			<p class="col-md-10 text-justify col-md-push-1">
				<%= classList[i]._description %>
			</p> <!--.col -->
		</div><!-- row --> 
		<div class="dropdown">
			<button class="btn btn-secondary btn-sm dropdown-toggle" type=button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopu="true" aria-expanded="false">
				Waitlisted
			</button>
			<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
				<h6 class="dropdown-header">Mark as...</h6>
				<li>
					<form method="post" action="/classes/completed" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Completed</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/take" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >In Progress</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/register" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Registered</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/waitlist" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Waitlisted</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/untake" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Still Needed</button>
					</form>
				</li>
			</div>
		</div>
	</div> <!-- list-group-item col-md-10 col-md-push-1 col-md-pull-1 -->
	<% } %>

	<% var classList = user.classesFinished %> <% var course_json = JSON.stringify(classList[i]) %>
	<% for (var i=0; i<classList.length; i++) { %>
	<div class="list-group-item col-md-10 col-md-push-1 col-md-pull-1">
		<div class="row bg-success">
			<div class="col-md-4">
				<h4><%= classList[i]._name %></h4>
			</div> <!--- .col ---!>
			<div class="col-md-3 pull-right">
				<h4 class="text-right"><%= classList[i]._subject %> <%=classList[i]._courseNumber %></h4>
			</div> <!--- .col ---!>
		</div> <!--- .row ---!>

		<div class="row">
			<p class="col-md-10 text-justify col-md-push-1">
				<%= classList[i]._description %>
			</p> <!--.col -->
		</div><!-- row --> 
		<div class="dropdown">
			<button class="btn btn-secondary btn-sm dropdown-toggle" type=button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopu="true" aria-expanded="false">
				Completed
			</button>
			<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
				<h6 class="dropdown-header">Mark as...</h6>
				<li>
					<form method="post" action="/classes/completed" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Completed</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/take" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >In Progress</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/register" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Registered</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/waitlist" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Waitlisted</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/untake" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Still Needed</button>
					</form>
				</li>
			</div>
		</div>
	</div> <!-- list-group-item col-md-10 col-md-push-1 col-md-pull-1 -->
	<% } %>

	<% var classList = classes_needed %> <% var course_json = JSON.stringify(classList[i]) %>
	<% for (var i=0; i<classList.length; i++) { %>
	<div class="list-group-item col-md-10 col-md-push-1 col-md-pull-1">
		<div class="row bg-danger">
			<div class="col-md-4">
				<h4><%= classList[i]._name %></h4>
			</div> <!--- .col ---!>
			<div class="col-md-3 pull-right">
				<h4 class="text-right"><%= classList[i]._subject %> <%=classList[i]._courseNumber %></h4>
			</div> <!--- .col ---!>
		</div> <!--- .row ---!>

		<% if (classList[i]._preRequisites.length > 0) { %>
			<div class="row">
				<p class="col-md-push-1 col-md-10 text-justify">
					Prerequisites:
					<%= classList[i]._preRequisites %>
				</p> <!--.col -->
			</div>
		<% } %>

		<div class="row">
			<p class="col-md-10 text-justify col-md-push-1">
				<%= classList[i]._description %>
			</p> <!--.col -->
		</div><!-- row --> 
		<div class="dropdown">
			<button class="btn btn-secondary btn-sm dropdown-toggle" type=button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopu="true" aria-expanded="false">
				Still Needed
			</button>
			<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
				<h6 class="dropdown-header">Mark as...</h6>
				<li>
					<form method="post" action="/classes/completed" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Completed</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/take" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >In Progress</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/register" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Registered</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/waitlist" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Waitlisted</button>
					</form>
				</li>
				<li>
					<form method="post" action="/classes/untake" class="inline">
					<button type="submit" name="mark_taken" value="<%= course_json %>" class="btn dropdown-item" >Still Needed</button>
					</form>
				</li>
			</div>
		</div>
	</div> <!-- list-group-item col-md-10 col-md-push-1 col-md-pull-1 -->
	<% } %>
	</ul> <!-- listgroup -->
</div>
</body>
</html>

